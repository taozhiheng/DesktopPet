<?xml version="1.0" encoding="utf-8"?>
<com.persist.desktoppet.ui.SlidingLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    app:contentIndex="1"
    app:autoScale="true"
    android:id="@+id/display_sliding"
    android:background="@mipmap/ic_drawer_bg"
    android:layout_width="match_parent"
    android:layout_height="match_parent">
    <LinearLayout
        android:layout_width="wrap_content"
        android:layout_height="match_parent"
        android:fitsSystemWindows="true"
        android:orientation="horizontal">
        <android.support.design.widget.NavigationView
            android:isScrollContainer="false"
            android:id="@+id/display_drawer"
            android:layout_width="273dp"
            android:layout_height="fill_parent"
            android:layout_gravity="start"
            android:padding="0dp"
            android:background="@null"
            app:itemTextColor="@color/bron"
            app:itemIconTint="@color/bron"
            app:headerLayout="@layout/drawer_header"
            app:menu="@menu/menu_drawer"
            />
        <include layout="@layout/display_content" />
    </LinearLayout>
</com.persist.desktoppet.ui.SlidingLayout>